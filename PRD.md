# 월간 자산 흐름 플래너

## Product Requirements & Technical Specification v1.0

> 문서 목적: 제품의 핵심 요구사항과 기술 구현 방향을 하나의 기준으로 통합하여, 본 문서를 기반으로 MVP를 실제 개발하고 향후 서비스화·수익화를 준비한다.
> 제품 상태: MVP 개발 기준
> 작성 기준: 2026-08-19
> 기반 문서: 통합 제품 명세서 v1.1 + 기술 스택 논의 결과

---

# 1. 제품 정의

## 1.1 한 문장 정의

**현재의 자산·부채 상태를 기준으로 미래의 재무 변화를 이벤트 단위로 계획하고, 그 결과 앞으로의 현금과 순자산이 어떻게 변하는지 보여주는 미래 중심 자산관리 서비스**

## 1.2 핵심 질문

> **지금 계획대로 생활하면 앞으로 내 돈은 언제·무엇 때문에·얼마나 달라지는가?**

이 서비스는 과거의 모든 거래를 기록하는 가계부가 아니다.

사용자가 실제로 기억하고 계획하는 수준의 중요한 재무 사건을 입력하고, 이를 시간축에 배치하여 미래의 현금과 순자산 흐름을 보여준다.

예:

* 8월 월급 +280만 원
* 8월 휴대폰 구매 -80만 원
* 9월 적금 만기 +1,000만 원
* 매월 생활비 -100만 원
* 10월 이사 비용 -300만 원
* 12월 투자계좌 회수 +500만 원

---

# 2. 제품 목표

## 2.1 핵심 목표

사용자가 미래의 재무 상태를 빠르게 이해하고, 큰 재무 결정을 하기 전에 그 결과를 확인할 수 있도록 한다.

## 2.2 MVP에서 검증할 핵심 가설

> **사용자는 세부 거래를 기록하지 않아도 월별 주요 사건만 입력하면 미래 재정 상태를 이해하고 실제 결정을 개선할 수 있다.**

## 2.3 MVP의 가장 중요한 경험

> **사건 하나를 추가하면, 앞으로 어느 달의 현금과 순자산이 왜 달라지는지 바로 이해할 수 있다.**

이 경험을 MVP의 최우선 성공 기준으로 삼는다.

---

# 3. 제품 원칙

### P-001. 미래 중심

과거 거래를 모두 복원하는 것이 목적이 아니다. 현재 상태를 정확하게 만들고 그 이후의 중요한 변화를 계획한다.

### P-002. 이벤트 중심

모든 거래를 기록하지 않는다.

월급, 생활비, 큰 구매, 적금 납입·만기, 대출 상환, 투자금 추가·회수 등 재무적으로 의미 있는 사건만 기록한다.

### P-003. 현재 상태와 미래 이벤트를 분리한다

현재 상태는 `Account`가 가지고, 시간에 따른 변화는 `Event`가 가진다.

### P-004. 입력은 단순해야 한다

기본 이벤트 입력은 다음을 중심으로 한다.

* 발생일
* 이름
* 금액
* 유형
* 계좌

반복 여부가 필요한 경우 추가한다.

메모, 세부 분류 등은 선택 항목으로 둔다.

이벤트는 정확한 발생일을 필수로 입력한다. 분석에 사용하는 월은 발생일에서 시스템이 자동으로 계산한다.

단, 이벤트 유형에 따라 필요한 추가 입력이 있을 수 있다.

### P-005. 분석은 단순해야 한다

사용자는 현재 기준 향후 3개월, 6개월, 12개월 중 분석 범위를 선택할 수 있다.

기본값은 향후 12개월이다.

### P-006. 입력 범위와 분석 범위를 분리한다

과거·현재·미래의 데이터 입력을 허용한다.

선택한 분석 범위 밖의 데이터도 저장할 수 있으며, 기본 화면의 분석 범위 기본값은 현재 기준 향후 12개월이다.

### P-007. 예측하지 않는다

서비스가 주가, 코인 가격, 투자수익률, 환율, 미래 자산가격을 임의로 예측하지 않는다.

미래 자산가치는 사용자가 입력한 계획과 마지막으로 확인된 평가값을 기반으로 한다.

### P-008. 판단을 대신하지 않는다

위험을 알려줄 수는 있지만 금융상품이나 투자행동을 추천하지 않는다.

예:

* 허용: "10월 현금이 40만 원 부족합니다."
* 금지: "투자를 줄이세요."

---

# 4. 사용자

## 4.1 MVP 사용자

초기에는 서비스 제작자가 직접 사용하는 개인용 도구로 시작한다.

목적은 실제 사용을 통해 다음을 검증하는 것이다.

* 입력 방식이 충분히 간단한가?
* 미래 흐름을 실제로 이해하는 데 도움이 되는가?
* 반복적으로 사용할 가치가 있는가?
* 어떤 기능에 사용자가 가장 큰 가치를 느끼는가?

## 4.2 향후 확장 사용자

* 가계부 기록이 번거로운 사람
* 여러 계좌를 보유한 사람
* 적금·대출·투자자산을 함께 관리하는 사람
* 큰 지출이나 이사 등을 미리 계획하고 싶은 사람
* 투자와 생활 현금의 관계를 함께 보고 싶은 사람

---

# 5. 핵심 도메인 모델

제품의 기본 구조는 다음과 같다.

```text
현재 상태
    ↓
Account
    ↓
미래 변화
    ↓
Event
    ↓
월별 계산
    ↓
Cash / Asset / Debt / Net Worth
    ↓
Risk / Event Contribution
    ↓
Scenario
```

---

# 6. Account

## 6.1 역할

사용자의 현재 자산·부채 상태를 표현하는 기본 단위다.

모든 돈은 계좌에 속한다.

계좌는 연결된 이벤트가 0개일 때만 삭제할 수 있다. 계좌 생성 시 입력한 초기 잔액은 삭제 가능 여부에 영향을 주지 않는다. 연결된 이벤트가 있는 계좌는 이벤트를 먼저 삭제하거나 계좌 연결을 해제해야 한다.

## 6.2 계좌 유형

| Type          | 설명             |
| ------------- | -------------- |
| `CASH`        | 입출금 계좌. 생활비 통장, 급여 통장, 비상금 통장 |
| `SAVINGS`     | 정기예금, 적금       |
| `INVESTMENT`  | 주식·ETF·암호화폐 계좌 |
| `DEBT`        | 신용대출, 전세대출     |
| `OTHER_ASSET` | 보증금, 차량, 기타 자산 |

## 6.3 유동성

계좌 유형과 별도로 `liquidity`를 관리한다.

* `LIQUID`
* `ILLIQUID`

유동성은 계좌 유형으로 자동 추론하지 않는다.

예:

* SAVINGS라도 만기 전 인출이 제한되면 ILLIQUID
* INVESTMENT라도 즉시 현금화 가능한 자산이면 LIQUID

이 속성은 자산 화면의 구분에 사용한다.

현금 부족 리스크는 MVP에서 `CASH` 계좌를 기준으로 판단한다.

---

# 7. 현재 상태

## 7.1 초기 입력

서비스 시작 시 과거 전체 거래를 입력하도록 요구하지 않는다.

예:

```text
2026-08 현재

생활비 통장 = 300만 원
비상금 통장 = 500만 원
투자계좌 = 1,500만 원
대출 = 800만 원
```

이 현재 상태에서 미래 계산을 시작한다.

## 7.2 과거 데이터

과거 사건은 현재 또는 미래 계산에 필요한 경우에만 선택적으로 입력한다.

과거 거래를 보존하기 위한 입력은 요구하지 않는다.

## 7.3 기준일

Account의 현재 상태에는 `as_of_date`를 저장한다.

MVP에서는 모든 Account가 동일한 기준일을 사용한다. 사용자는 현재 상태 기준일을 한 번 입력하며, 시스템은 각 Account에 동일한 `as_of_date`를 저장한다.

계산은 해당 기준 상태 이후의 이벤트를 시간 순서대로 반영한다.

---

# 8. Event

## 8.1 역할

Event는 시간에 따른 재무 변화를 표현하는 최소 사용자 기록 단위다.

사용자는 회계적 의미가 아니라 "무슨 일이 일어났는가"를 입력한다.

```text
사용자 입력
    ↓
Event
    ↓
원장 효과
    ↓
계좌 잔액
    ↓
순자산
```

이벤트와 회계 효과를 분리한다.

## 8.2 이벤트 유형

| Type                       | 의미         |
| -------------------------- | ---------- |
| `INCOME`                   | 수입         |
| `EXPENSE`                  | 지출         |
| `TRANSFER`                 | 계좌 간 이동    |
| `INVESTMENT_CONTRIBUTION`  | 투자계좌 자금 추가 |
| `INVESTMENT_WITHDRAWAL`    | 투자계좌 자금 회수 |
| `ASSET_VALUE_UPDATE`       | 자산 평가금액 갱신 |
| `DEBT_DRAW`                | 대출 실행      |
| `DEBT_PRINCIPAL_REPAYMENT` | 대출 원금 상환   |
| `DEBT_INTEREST`            | 대출 이자 납부   |
| `SAVINGS_MATURITY`         | 예·적금 만기    |

## 8.3 기본 입력

공통 필드:

```text
event_date
name
amount
type
```

단일 계좌 이벤트는 이벤트 유형에 따라 출금 또는 입금 Account 하나를 사용한다. 두 Account에 영향을 주는 이벤트는 `source_account`와 `destination_account`를 사용한다. 이들은 Account 유형이 아니라 각 이벤트 안에서의 역할이다.

`month`는 `event_date`에서 시스템이 자동으로 계산하는 내부 필드다. 같은 날짜의 이벤트는 사용자 입력 순서와 무관하게 하나의 묶음으로 합산 반영한다.

추가 필드가 필요한 이벤트는 유형별로 확장한다.

예:

`TRANSFER`

```text
source_account
destination_account
```

`INVESTMENT_WITHDRAWAL`

```text
pre_withdrawal_value
```

## 8.4 MVP 이벤트 영향 규칙

MVP에서 금액은 항상 양수로 입력하며, 증감 방향은 Event 유형이 결정한다.

* 현금은 모든 `CASH` Account 잔액의 합계다.
* 자산은 `CASH`, `SAVINGS`, `INVESTMENT`, `OTHER_ASSET` Account 잔액의 합계다.
* 부채는 `DEBT` Account 잔액의 합계다.
* 순자산은 자산 합계에서 부채 합계를 뺀 값이다.

| Event 유형 | MVP 처리 | 허용 Account 조합 | Account 잔액 효과 | 현금 | 자산 | 부채 | 순자산 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `INCOME` | 지원 | 받는 Account: `CASH` 또는 `SAVINGS` | 받는 Account 증가 | CASH 수령 시 증가 | 증가 | 변화 없음 | 증가 |
| `EXPENSE` | 지원 | 사용하는 Account: `CASH` | 사용하는 Account 감소 | 감소 | 감소 | 변화 없음 | 감소 |
| `TRANSFER` | 지원 | `CASH ↔ CASH`, `CASH ↔ SAVINGS`, `SAVINGS ↔ SAVINGS` | 출발 Account 감소, 도착 Account 증가 | CASH 포함 여부에 따라 증감 | 변화 없음 | 변화 없음 | 변화 없음 |
| `INVESTMENT_CONTRIBUTION` | 제한 지원 | 출발: `CASH`, 도착: `INVESTMENT` | CASH 감소, 투자 Account 증가 | 감소 | 변화 없음 | 변화 없음 | 변화 없음 |
| `DEBT_DRAW` | 지원 | 부채: `DEBT`, 받는 Account: `CASH` | DEBT 증가, CASH 증가 | 증가 | 증가 | 증가 | 변화 없음 |
| `DEBT_PRINCIPAL_REPAYMENT` | 지원 | 사용하는 Account: `CASH`, 부채: `DEBT` | CASH 감소, DEBT 감소 | 감소 | 감소 | 감소 | 변화 없음 |
| `DEBT_INTEREST` | 지원 | 사용하는 Account: `CASH` | CASH 감소 | 감소 | 감소 | 변화 없음 | 감소 |
| `SAVINGS_MATURITY` | 지원 | 출발: `SAVINGS`, 도착: `CASH` | SAVINGS 감소, CASH 증가 | 증가 | 변화 없음 | 변화 없음 | 변화 없음 |

추가 제약:

* `CASH → SAVINGS`의 저축·적금 납입은 `TRANSFER`로 기록한다.
* `SAVINGS → CASH`의 만기 수령은 `SAVINGS_MATURITY`로, 중도 인출 또는 일반 이동은 `TRANSFER`로 기록한다.
* `CASH → INVESTMENT`는 반드시 `INVESTMENT_CONTRIBUTION`으로 기록한다. 이 이벤트는 Risk Guard에 필요한 현금 이동만 MVP에서 반영하며, 투자 원금·손익 계산은 포함하지 않는다.
* `DEBT → CASH`는 반드시 `DEBT_DRAW`로, `CASH → DEBT`는 반드시 `DEBT_PRINCIPAL_REPAYMENT`로 기록한다.
* `DEBT` Account를 일반 `TRANSFER`에 사용하는 것은 허용하지 않는다.
* `OTHER_ASSET`은 MVP에서 현재 상태와 순자산에만 반영하며, 잔액 이동과 평가금액 갱신은 지원하지 않는다.
* `INVESTMENT_WITHDRAWAL`과 `ASSET_VALUE_UPDATE`는 Phase 3 이후에 지원한다.

---

# 9. 계획과 실제

이벤트는 다음 상태를 가진다.

* `PLANNED`
* `CONFIRMED`
* `CHANGED`
* `CANCELLED`

## 계산 원칙

* 미래: 계획값 사용
* 지나간 월: 확정값 우선
* 계획과 실제가 달라진 경우 변경 상태로 관리
* 취소된 계획은 계산에서 제외

월 마감 시 미확정 계획이 남아 있으면 자동 확정하지 않고 사용자에게 확인을 요구한다.

---

# 10. 반복 이벤트

반복 이벤트를 지원한다.

예:

```text
월급 +280만 원
매월 생활비 -100만 원
적금 납입 -50만 원
```

반복 규칙은 Event의 `recurrence_rule`로 관리한다.

반복 이벤트를 실제 계산에 반영할 때는 각 대상 월의 이벤트로 확장한 뒤 일반 이벤트와 동일한 계산 엔진을 적용한다.

---

# 11. 계산 엔진

## 11.1 기본 구조

```text
원본 데이터
    ↓
이벤트 구성
    ↓
월별 이벤트 정렬
    ↓
월별 계좌 잔액 계산
    ↓
자산 합계
    ↓
부채 합계
    ↓
순자산
    ↓
리스크 계산
    ↓
Event Contribution
```

## 11.2 계산 순서

각 월에 대해:

1. 전월 말 계좌 상태를 가져온다.
2. 해당 월의 Event를 날짜순으로 정렬하고, 같은 날짜 Event는 하나의 묶음으로 반영한다.
3. 해당 월의 확정 Event를 반영한다.
4. 미래 월에는 계획 Event를 반영한다.
5. 계좌별 월말 잔액을 계산한다.
6. 자산 합계를 계산한다.
7. 부채 합계를 계산한다.
8. 순자산을 계산한다.
9. 위험 규칙을 검사한다.
10. 각 결과의 원인이 된 이벤트를 추적한다.

## 11.3 핵심 공식

```text
월말 현금
= 전월 말 현금
+ 현금 유입
- 현금 유출
```

```text
월말 순자산
= 자산 합계
- 부채 합계
```

계좌 간 이동은 전체 순자산을 변화시키지 않는다.

---

# 12. 투자계좌

투자계좌 내부의 종목별 매수·매도는 기록하지 않는다.

기록 대상:

* 투자금 추가
* 투자금 회수
* 평가금액
* 투자 종료

## 12.1 부분 회수

회수 직전:

```text
B = 남은 투입원금
V = 회수 직전 평가금액
W = 회수금
```

회수비율:

```text
W / V
```

회수원금:

```text
B × W / V
```

실현손익:

```text
W - 회수원금
```

회수 후 남은 원금:

```text
B - 회수원금
```

회수 후 평가금액:

```text
V - W
```

회수 직전 평가금액이 없으면 정확한 원금·손익 분리가 불가능하므로 회수 등록 시 입력을 요구한다.

## 12.2 교차검증

```text
누적손익
= 현재 평가금액
+ 누적 회수금
- 누적 투입금
```

```text
누적손익
= 누적 실현손익
+ 현재 미실현손익
```

---

# 13. 미래 자산가치

서비스는 미래 시장가격을 예측하지 않는다.

따라서:

```text
최근 확정 평가금액
+
계획된 추가 투자
-
계획된 회수
=
미래 예상 평가금액
```

구조를 기본으로 한다.

이 값은 시장 예측값이 아니라 **입력된 계획을 기준으로 한 예상치**다.

---

# 14. Risk Guard

Risk Guard는 행동을 추천하는 기능이 아니다.

다음 네 가지를 보여준다.

1. 무엇이 발생하는가?
2. 언제 발생하는가?
3. 어떤 계좌에 영향을 주는가?
4. 얼마나 부족하거나 변화하는가?

## 14.1 MVP 필수 규칙

### 현금 부족

예:

> "10월 생활비 계좌가 40만 원 부족합니다."

`CASH` 계좌 기준으로 판단한다.

날짜별 이벤트 묶음을 반영한 뒤 `CASH` Account 잔액 합계가 음수가 되면 현금 부족 Risk를 생성한다. 이후 잔액이 회복되더라도 Risk는 유지하며, 최저 현금, 발생일, 원인이 된 Event를 보여준다.

### 비상자금 부족

사용자가 월 생활비와 목표 개월 수를 설정한다.

예:

```text
월 생활비 = 150만 원
비상자금 기준 = 3개월
목표 = 450만 원
```

비상자금 대상 계좌에는 `emergency_fund_eligible` 속성을 둔다.

### 투자자금 이동 후 현금 부족

예:

> "투자계좌에 500만 원을 추가한 후 사용 가능한 현금이 안전 기준보다 낮아집니다."

## 14.2 향후 규칙

MVP 이후:

* 부채상환 부담
* 큰 지출 집중
* 오래된 평가금액

등을 추가한다.

---

# 15. 설명 가능성

모든 계산 결과는 원인이 된 이벤트를 추적할 수 있어야 한다.

예:

```text
10월 현금 = 40만 원

+280만 월급
-100만 생활비
-300만 이사
= -120만 순변화
```

사용자가 그래프의 특정 월을 선택하면 변화의 원인을 확인할 수 있어야 한다.

---

# 16. Scenario

## 16.1 목적

기본 계획을 변경하지 않고 대안 계획을 만들어 비교한다.

예:

```text
기본 계획
휴대폰 8월 구매

Scenario A
휴대폰 10월 구매
```

## 16.2 구조

Scenario는 기본 계획을 직접 복제하지 않고 `Override` 레이어를 사용한다.

```text
Base Events
    ↓
Override
    ↓
Final Events
    ↓
Calculation Engine
```

계산 엔진 자체는 변경하지 않는다.

## 16.3 Override 유형

```text
MOVE
MODIFY_AMOUNT
REMOVE
ADD
```

하나의 이벤트에서 시기와 금액을 동시에 변경하는 경우 각각 별도의 Override를 생성한다.

## 16.4 비교

최대 약 3개 계획을 비교한다.

핵심 지표:

* 연말 예상 순자산
* 기간 중 최저 현금
* 최저 현금 발생 월
* 위험 발생 여부
* 위험 발생 월
* 변경된 이벤트

모든 월별 데이터를 나열하지 않는다.

## 16.5 채택

사용자가 시나리오를 채택하면 변경사항을 기본 계획에 반영한다.

단, 시나리오 생성 이후 기본 계획이 변경된 경우 자동 덮어쓰지 않는다.

충돌이 발생한 Override만 사용자에게 확인한다.

---

# 17. 정보 구조

기본 탐색 메뉴:

```text
홈
월별 계획
계좌
시나리오
```

사건 추가는 공통 주요 행동으로 접근 가능해야 한다.

---

# 18. 주요 화면

## 18.1 홈

핵심 정보:

1. 현재 현금
2. 현재 순자산
3. 선택한 분석 기간의 현금 흐름
4. 선택한 분석 기간의 순자산 흐름
5. 가장 가까운 위험
6. 다음 주요 사건

가장 중요한 정보는 상단 숫자가 아니라 **선택한 분석 기간의 흐름**이다.

---

## 18.2 월별 계획

월을 선택하면 해당 월의 이벤트를 보여준다.

기본 입력:

```text
발생일
이름
금액
유형
계좌
반복 여부
```

분석에 사용하는 월은 발생일에서 시스템이 자동으로 계산한다. 메모·세부 분류는 필요할 경우 추가 입력한다.

월말에는 계획을 실제 결과로 확정할 수 있다.

---

## 18.3 계좌

자산과 부채를 구분하여 보여준다.

계좌 상세에서는 유형에 따라:

* 잔액
* 평가금액
* 투자금 추가
* 회수
* 평가금액 갱신
* 대출 상환

등을 제공한다.

---

## 18.4 시나리오

기본 계획과 대안 계획의 핵심 차이를 비교한다.

결과가 달라진 지표와 원인이 된 이벤트를 우선 표시한다.

---

# 19. 최소 데이터 모델

## Account

```text
id
name
type
currency
current_balance
as_of_date
emergency_fund_eligible
liquidity
```

## Event

```text
id
month
event_date
name
type
amount
source_account
destination_account
status
recurrence_rule
note
```

## ValuationSnapshot

```text
id
account_id
month
valuation
```

## InvestmentFlow

```text
id
account_id
month
direction
amount
pre_withdrawal_value
```

## Scenario

```text
id
name
base_plan_version
overrides
status
```

## Override

```text
id
scenario_id
operation
target_event_id
target_event_snapshot
new_month
new_amount
new_event
```

## RiskRule

```text
id
type
threshold
enabled
```

---

# 20. 원본 데이터와 계산 결과

## 원본 데이터

```text
Account
Event
ValuationSnapshot
InvestmentFlow
RiskRule
Scenario
Override
```

## 계산 결과

```text
MonthlyBalance
MonthlyCash
MonthlyAssets
MonthlyDebt
MonthlyNetWorth
RiskResult
EventContribution
```

계산 결과는 파생 데이터다.

사용자가 직접 수정할 수 없다.

원본 데이터가 변경되면 다시 계산한다.

---

# 21. 기술 스택

## 21.1 확정 기술

| 영역                  | 기술             |
| ------------------- | -------------- |
| Backend Language    | Python         |
| Backend Framework   | FastAPI        |
| Frontend            | React          |
| Database            | MySQL          |
| Container           | Docker         |
| Local Orchestration | Docker Compose |
| Repository          | GitHub         |
| 1차 배포               | Cloudtype      |

이 선택은 개발자의 기존 Python·MySQL 경험을 최대한 활용하면서 실제 서비스까지 빠르게 구현하는 것을 우선한다.

---

# 22. Frontend 기술

## 22.1 React

제품은 로그인 이후 사용하는 애플리케이션 중심 서비스이며 다음과 같은 인터랙션이 중요하다.

* 3개월, 6개월, 12개월 그래프
* 월 선택
* 이벤트 추가·수정
* 시나리오 비교
* 결과 변경 확인

따라서 React 기반 SPA 구조를 사용한다.

## 22.2 React tooling

현재 권장안:

> **React + Vite**

Next.js는 향후 SEO 중심의 공개 웹페이지나 서버 렌더링이 필요해질 경우 재검토한다.

MVP에서는 FastAPI를 별도 Backend로 두므로 React + Vite의 단순한 구조를 우선한다.

---

# 23. UI 기술

권장:

```text
Tailwind CSS
+
shadcn/ui
```

목적은 UI 개발 시간을 줄이고 일관된 컴포넌트 체계를 확보하는 것이다.

초기에는 디자인 시스템을 과도하게 구축하지 않는다.

제품의 핵심은 시각적 화려함보다:

* 숫자의 명확성
* 흐름의 이해
* 사건과 결과의 연결
* 위험의 인지

다.

---

# 24. Backend 구조

FastAPI를 API 계층으로 사용한다.

```text
React
   ↓
REST API
   ↓
FastAPI
   ↓
Application Service
   ↓
Calculation Engine
   ↓
Repository
   ↓
MySQL
```

## 핵심 원칙

**계산 엔진과 API를 분리한다.**

FastAPI가 계산 로직을 직접 소유하지 않는다.

계산 엔진은 독립적인 Python 도메인 로직으로 구성한다.

---

# 25. ORM 및 Database

## ORM

권장:

> **SQLAlchemy**

## Migration

권장:

> **Alembic**

구조:

```text
FastAPI
   ↓
SQLAlchemy
   ↓
MySQL
```

Alembic을 통해 DB schema 변경을 migration으로 관리한다.

---

# 26. API

MVP에서는 REST API를 사용한다.

예시:

```text
GET    /accounts
POST   /accounts
PATCH  /accounts/{id}
DELETE /accounts/{id}

GET    /events
POST   /events
PATCH  /events/{id}
DELETE /events/{id}

GET    /forecast
GET    /risks

GET    /scenarios
POST   /scenarios
GET    /scenarios/{id}
POST   /scenarios/{id}/adopt
```

실제 API 명세는 개발 과정에서 도메인 모델 확정 후 구체화한다.

---

# 27. Frontend 서버 상태 관리

권장:

> **TanStack Query**

서버에서 관리되는:

* Account
* Event
* Scenario
* Forecast
* Risk

등을 React 상태와 분리하여 관리한다.

클라이언트 UI 상태와 서버 데이터를 구분한다.

---

# 28. 로컬 개발 환경

Docker 기반으로 개발한다.

권장 구조:

```text
monthly-cashflow-planner/
│
├── frontend/
│   └── React + Vite
│
├── backend/
│   └── FastAPI
│
├── mysql/
│
├── docker-compose.yml
│
├── docs/
│
└── README.md
```

개발자는 로컬 환경에서 전체 서비스를 Docker Compose로 실행할 수 있어야 한다.

---

# 29. 배포 구조

## 29.1 MVP 배포

초기 배포 플랫폼은 **Cloudtype**으로 한다.

```text
                    Internet
                       │
                       ▼
                 Cloudtype
                       │
             ┌─────────┴─────────┐
             ↓                   ↓
        React Frontend       FastAPI Backend
                                   │
                                   ↓
                                MySQL
```

애플리케이션은 Docker 기반으로 패키징한다.

## 29.2 배포 원칙

개발 환경과 운영 환경에서 애플리케이션의 실행 구조를 최대한 동일하게 유지한다.

```text
Local Docker
     ↓
Cloudtype Docker
```

Docker를 통해 배포 환경에 대한 종속성을 낮춘다.

---

# 30. Database 배포 전략

초기에는 Cloudtype 환경에서 MySQL을 사용할 수 있다.

다만 실제 유료 사용자와 서비스 데이터가 축적되기 시작하면 DB를 별도의 Managed Database로 분리하는 것을 우선 검토한다.

예:

```text
Cloudtype
 ├── Frontend
 └── Backend

Managed MySQL
 └── Database
```

후보:

* AWS RDS
* GCP Cloud SQL
* 기타 Managed MySQL

현재 단계에서는 어느 서비스를 선택할지 확정하지 않는다.

---

# 31. AWS / GCP 이전 전략

AWS 또는 GCP는 현재 배포 플랫폼으로 확정하지 않는다.

필요성이 발생할 경우 Docker 기반 애플리케이션을 그대로 이전하는 것을 기본 전략으로 한다.

```text
Cloudtype
    ↓
Docker Image
    ↓
AWS / GCP
```

이때 애플리케이션 코드의 변경을 최소화한다.

이 구조의 목적은 초기 단계에서 인프라 운영에 시간을 쓰지 않고, 제품 검증 이후 필요한 만큼 인프라를 확장하는 것이다.

---

# 32. 인증

MVP 초기에는 개인용 사용을 우선한다.

따라서 인증 시스템은 계산 엔진 및 핵심 기능 검증 이후 추가할 수 있다.

단, SaaS 확장을 고려하여 주요 데이터 모델은 처음부터 사용자 단위로 분리할 수 있는 구조를 유지한다.

향후 후보:

* Google Login
* Email Login
* 외부 인증 서비스

구체적인 인증 방식은 MVP의 실제 공개 시점에 결정한다.

---

# 33. 보안 원칙

이 서비스는 사용자의 자산·부채·수입·지출 정보를 다루므로 초기부터 기본적인 보안 원칙을 적용한다.

필수:

* HTTPS
* 사용자별 데이터 격리
* DB 접근 권한 분리
* Secret / API Key 환경변수 관리
* 금융기관 비밀번호 저장 금지
* 로그에 자산정보를 불필요하게 기록하지 않기
* 데이터 삭제 가능 구조
* DB 백업

은행·증권사 자동연동은 MVP 범위에 포함하지 않으므로 금융기관 인증정보를 직접 저장할 필요가 없다.

---

# 34. 테스트

## 34.1 계산 엔진 테스트

다음은 자동화 테스트로 구축한다.

* 계좌 간 이동
* 투자금 추가
* 투자금 회수
* 부분 회수 원금·손익 계산
* 대출 실행
* 대출 원금 상환
* 대출 이자
* 계획 → 실제 전환
* 이벤트 취소
* 반복 이벤트
* 계좌 추가
* 리스크 규칙

## 34.2 테스트 도구

Backend:

> pytest

Frontend:

> Vitest

핵심 계산 로직은 높은 테스트 커버리지를 목표로 한다.

---

# 35. CI/CD

GitHub를 기준으로 관리한다.

기본 흐름:

```text
개발
 ↓
Git Commit
 ↓
GitHub Push
 ↓
Automated Test
 ↓
Docker Build
 ↓
Deploy
```

GitHub Actions를 사용하여 테스트 및 배포 자동화를 구축한다.

초기에는 수동 배포가 가능하지만 MVP 안정화 이후 자동 배포를 기본으로 한다.

---

# 36. 개발 단계

## Phase 0 — 개발 기반

목표:

> 실제 개발 가능한 환경 구축

범위:

* GitHub Repository
* Docker
* Docker Compose
* React + Vite
* FastAPI
* MySQL
* SQLAlchemy
* Alembic
* 기본 CI

완료 조건:

```text
docker compose up
```

으로 Frontend / Backend / MySQL이 함께 실행된다.

---

## Phase 1 — Calculation Engine

목표:

> 제품의 핵심 계산이 정확하게 동작한다.

범위:

* Account
* Event
* 3개월, 6개월, 12개월 계산
* Cash
* Asset
* Debt
* Net Worth
* Event Contribution
* 핵심 테스트

UI는 최소화한다.

계산 엔진을 먼저 검증한다.

---

## Phase 2 — 개인용 MVP

범위:

* 계좌 생성·수정·삭제
* 이벤트 입력
* 반복 이벤트
* 현재 상태 입력
* 홈 타임라인
* 월별 계획
* 계획·실제
* 월말 확정
* Risk Guard
* 투자자금 이동 후 현금 부족 판단을 위한 `INVESTMENT_CONTRIBUTION` 반영

목표:

> 실제 사용자가 자신의 재무 데이터를 입력하여 사용할 수 있는 상태

---

## Phase 3 — Investment

범위:

* 투자금 추가 내역의 원금 관리
* 투자금 회수
* 회수 직전 평가금액
* 원금·실현손익 계산
* 평가 스냅샷
* 투자계좌 상세

---

## Phase 4 — Scenario

범위:

* 기본 계획
* Scenario 생성
* Override
* 최대 3개 비교
* 핵심 결과 비교
* 충돌 감지
* Scenario 채택

---

## Phase 5 — 실사용 검증

목표:

> 제작자 본인이 실제 생활에서 반복 사용한다.

검증:

* 사건 추가 빈도
* 계획 수정 빈도
* 위험 확인 빈도
* 월말 확정 과정
* 실제 의사결정에 도움이 되는지

---

## Phase 6 — 공개 MVP

조건:

* 핵심 계산 안정성 확보
* 기본 UX 완성
* 사용자 데이터 격리
* 인증
* 백업
* 배포 안정성
* 기본 모니터링

이후 소규모 외부 사용자에게 공개한다.

---

# 37. 수익화 전략

초기 목표는 즉시 수익 극대화가 아니다.

우선 다음 순서로 검증한다.

```text
제품 구현
 ↓
실제 사용
 ↓
반복 사용
 ↓
사용자가 느끼는 핵심 가치 발견
 ↓
외부 사용자 검증
 ↓
유료 가치 검증
 ↓
결제
```

## 초기 유료화 후보

향후 다음 기능을 Pro 기능으로 검토할 수 있다.

* 고급 시나리오 비교
* 무제한 계획 관리
* 고급 Risk Guard
* 장기 계획
* 데이터 Export
* 여러 계획 관리

단, 실제 사용 데이터를 확인하기 전에는 유료 기능을 확정하지 않는다.

---

# 38. 서비스 확장 방향

장기적으로 다음 방향을 고려한다.

```text
개인용 자산 흐름 플래너
        ↓
개인 재무 계획 SaaS
        ↓
시나리오 기반 재무 의사결정 도구
```

확장 후보:

* 사용자별 계정
* 여러 계획
* 가족/공동 자산
* 금융기관 연동
* 자동 평가값 갱신
* 고급 리스크 분석
* 데이터 Export
* 모바일 최적화
* 결제/구독

단, 이 기능들은 MVP에 포함하지 않는다.

---

# 39. MVP 제외 범위

다음 기능은 MVP에서 구현하지 않는다.

* 투자계좌 원금·실현손익 계산과 평가 스냅샷
* 투자금 회수
* Scenario 생성·비교·채택
* 일별 가계부
* 카드 거래 자동 분류
* 은행 자동연동
* 증권사 자동연동
* 종목별 매매 기록
* 실시간 시세
* 미래 투자수익률 예측
* 환율 예측
* 세금 계산
* 금융상품 추천
* 투자행동 추천
* 복잡한 자산배분
* 복잡한 위험통계
* AI 재무상담

---

# 40. 핵심 성공 지표

## 사용성

| 지표            |     목표 |
| ------------- | -----: |
| 첫 계획 완성       | 10분 이내 |
| 새 사건 추가       | 30초 이내 |
| 선택한 분석 기간의 위험 시점 파악 | 10초 이내 |
| 월말 실제 확정      |  3분 이내 |

## 사용성 검증

* 주 1회 이상 사용
* 한 달 뒤 계획 수정 또는 유지
* 실제 재무 의사결정에 사용

## 제품 가치 검증

다음 질문에 긍정적으로 답할 수 있어야 한다.

> "이 서비스가 없었으면 놓쳤을 미래의 현금 부족이나 재무 변화를 발견했는가?"

---

# 41. 계산 검증 기준

최소한 다음 테스트를 통과해야 MVP 계산 엔진을 신뢰할 수 있다.

### T-001

계좌 간 이동 시 전체 순자산이 변하지 않는다.

### T-002

투자금 추가만으로 투자수익이 발생하지 않는다.

### T-003

부분 회수 시 회수 당시 비율에 따라 원금과 실현손익이 분리된다.

### T-004

회수금이 현금계좌와 전체 자산 흐름에 중복 반영되지 않는다.

### T-005

계획 이벤트 취소 시 이후 모든 월의 잔액이 재계산된다.

### T-006

실제 결과 확정 후 미래 계산이 확정값을 기준으로 이어진다.

### T-007

대출 실행 시 현금과 부채가 동시에 증가하고 순자산은 변하지 않는다.

### T-008

대출 원금 상환 시 현금과 부채가 동시에 감소하고 순자산은 변하지 않는다.

### T-009

대출 이자 납부 시 현금과 순자산이 감소한다.

### T-010

동일 원본 데이터에 대해 계산 결과가 항상 동일하다.

### T-011

현금 부족 Risk가 올바른 월과 계좌를 표시한다.

### T-012

투자금 이동 후 현금 부족 Risk가 올바르게 계산된다.

---

# 42. 개발 우선순위 원칙

개발 과정에서 다음 우선순위를 유지한다.

```text
정확성
  >
핵심 경험
  >
사용성
  >
확장성
  >
장식
```

특히 계산 정확성을 위해 UI 개발보다 Calculation Engine의 검증을 우선한다.

---

# 43. 기술적 핵심 원칙

## 43.1 Deterministic

동일한 원본 데이터는 항상 동일한 계산 결과를 만들어야 한다.

## 43.2 Explainable

계산 결과의 각 변화는 원인이 된 이벤트로 추적 가능해야 한다.

## 43.3 Source of Truth 분리

원본 데이터와 계산 결과를 분리한다.

## 43.4 Stateless Calculation

가능한 경우 계산 엔진은 DB 상태에 직접 의존하지 않고 전달받은 원본 데이터로 결과를 계산한다.

## 43.5 Docker First

개발과 배포 환경의 차이를 최소화한다.

## 43.6 Cloud Agnostic

특정 클라우드의 기능에 애플리케이션 핵심 로직을 강하게 종속시키지 않는다.

---

# 44. 전체 시스템 아키텍처

```text
                         User
                           │
                           ▼
                    ┌─────────────┐
                    │ React + Vite│
                    │  Frontend   │
                    └──────┬──────┘
                           │
                        REST API
                           │
                           ▼
                    ┌─────────────┐
                    │   FastAPI   │
                    │   Backend   │
                    └──────┬──────┘
                           │
               ┌───────────┴───────────┐
               │                       │
               ▼                       ▼
       ┌───────────────┐       ┌─────────────────┐
       │ Application   │       │ Calculation     │
       │ Services      │──────▶│ Engine          │
       └───────┬───────┘       └─────────────────┘
               │
               ▼
        ┌───────────────┐
        │ SQLAlchemy    │
        │ + Alembic     │
        └───────┬───────┘
                │
                ▼
           ┌─────────┐
           │  MySQL  │
           └─────────┘
```

---

# 45. 배포 아키텍처

## 현재

```text
GitHub
   │
   ▼
Cloudtype
   │
   ├── React Frontend
   │
   ├── FastAPI Backend
   │
   └── MySQL
```

## 향후 확장

```text
GitHub
   │
   ▼
Cloudtype / AWS / GCP
   │
   ├── Frontend
   │
   └── Backend
          │
          ▼
     Managed MySQL
```

서비스 규모와 운영 요구가 증가했을 때 인프라를 단계적으로 분리한다.

---

# 46. 현재 확정사항

### 제품

* 미래 중심 자산관리
* 월 단위
* 이벤트 중심
* Account 기반
* 계획 / 실제 분리
* 3개월 / 6개월 / 12개월 분석 범위 선택 (기본 12개월)
* Risk Guard
* Phase 2 범위로 MVP 한정

### 기술

* Python
* FastAPI
* React
* React + Vite 권장
* MySQL
* SQLAlchemy 권장
* Alembic 권장
* Docker
* Docker Compose
* GitHub
* Cloudtype

---

# 47. 아직 결정하지 않는 사항

다음은 실제 개발을 진행하면서 결정한다.

* 인증 방식
* 결제 시스템
* 구독 모델
* AWS vs GCP
* Managed MySQL 제공자
* 모니터링 서비스
* 이메일 시스템
* 파일 저장소
* 모바일 앱
* 금융기관 API 연동
* 고급 분석 기능

이들은 현재 MVP의 핵심 경험을 구현하는 데 필요하지 않다.

---

# 48. 최종 제품 방향

이 제품의 경쟁력은 금융 데이터를 많이 모으는 데 있지 않다.

사용자의 머릿속에 흩어진 재무 계획을 **월별 사건으로 바꾸고**, 그 결과를 **계좌 잔액·현금·순자산·위험 시점으로 번역**하는 데 있다.

기술적으로는:

```text
Python
+
FastAPI
+
React
+
MySQL
+
Docker
+
Cloudtype
```

이라는 단순하고 익숙한 기반으로 시작한다.

제품이 검증되기 전까지 불필요하게 복잡한 인프라를 구축하지 않는다.

사용자가 늘고 실제 수익화 가능성이 확인되면:

```text
Cloudtype
    ↓
Managed Database
    ↓
AWS / GCP 확장
```

순서로 인프라를 발전시킨다.

---

# 49. MVP 개발의 최종 목표

첫 번째 완성 제품은 다음 경험을 제공해야 한다.

> **현재 내 계좌와 잔액을 입력한다.**
>
> **앞으로 일어날 중요한 사건을 입력한다.**
>
> **선택한 기간의 현금과 순자산 흐름을 본다.**
>
> **위험한 달을 발견한다.**
>
> **그래프를 눌러 왜 그런 결과가 발생했는지 확인한다.**
>
> **계획을 수정하고 결과 변화를 확인한다.**

그리고 이 경험을 실제 사용자가 반복적으로 사용할 가치가 있는지 검증한다.

**제품의 첫 번째 성공 기준은 수익이 아니라 반복 사용이다.**

반복 사용이 확인된 이후 유료 기능과 구독 모델을 실험하여 수익화를 검증한다.
